<% @posts.each do |post| %>
  <article>
    <header>
      <h2><%= post.title %></h2>
      <small>Written by <%= post.user.name %> <%= time_ago_in_words(Time.now) %></small> 
    </header>

    <p><%= post.body.truncate(600) %></p>
  </article>
<% end %>